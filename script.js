// This function manipulates the DOM to create a grid of divs that is gridLength x gridWidth.
function createGrid(gridLength, gridWidth) {

}

const GRID_LENGTH = 16;
const GRID_WIDTH = 16;

createGrid(GRID_LENGTH, GRID_WIDTH);